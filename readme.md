This project is used for me to explore different types of random generation and their algorithms and to easily select each one independently.


Install Process:

Only tkinter is used

pip install -r requirements.txt